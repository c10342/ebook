
export const fileName = state => state.fileName

export const menuVisible = state => state.menuVisible

export const settingVisible = state => state.settingVisible

export const defaultFontSize = state => state.defaultFontSize

export const defaultFontFamily = state => state.defaultFontFamily

export const fontFamilyVisible = state => state.fontFamilyVisible

export const defaultTheme = state => state.defaultTheme

export const bookAvailable = state => state.bookAvailable

export const progress = state => state.progress

export const section = state => state.section

export const isPaginating = state => state.isPaginating

export const currentBook = state => state.currentBook

export const cover = state => state.cover

export const metadata = state => state.metadata

export const paginate = state => state.paginate

export const pagelist = state => state.pagelist

export const offsetY = state => state.offsetY

export const isBookmark = state => state.isBookmark

export const navigation = state => state.navigation
